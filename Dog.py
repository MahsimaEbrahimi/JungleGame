# Input string
input_string = "Hello, world! This is a test."

# Split the string into a list of substrings
substrings = input_string.split()

# Loop over the list using list comprehension
result = [k for k in substrings]

# Print the result
print(result)